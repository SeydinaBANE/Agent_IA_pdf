# 📦 Librairies principales pour le deep learning
torch>=2.0                      # Moteur de calcul (PyTorch)
transformers>=4.38             # Modèles pré-entraînés de Hugging Face

# ✂️ Support des tokenizers (tokenisation de texte)
sentencepiece                  # Utilisé par CamemBERT, MarianMT, etc.
tokenizers                     # Tokenizers rapides (en Rust)

# 🔁 Affichage de la progression dans les notebooks
tqdm                           # Barres de chargement
ipywidgets                     # Widgets interactifs dans Jupyter

# 📓 Interface de notebook Jupyter
jupyter                        # Pour exécuter des notebooks .ipynb

# 🔐 (Optionnel) Tokenizer OpenAI-like (peu utile ici mais préventif)
tiktoken                       # Tokenisation OpenAI (si jamais nécessaire)

# 📄 Lecture de documents PDF
PyMuPDF                        # Pour lire et extraire du texte depuis un fichier PDF

# 🌐 Interface web interactive
gradio                         # Pour créer une interface dans le navigateur
